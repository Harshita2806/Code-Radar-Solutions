#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int a, n;
    scanf("%d",&a);
    if (a > 1) 
        if (a % n == 0)
        printf("Not Prime");
        else
        printf("Prime");
    else
    printf("Prime");
    return 0;
}