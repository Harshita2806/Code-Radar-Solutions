#include <stdio.h>

int main() {
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c)
    int sum;
    sum = a+b+c;
    float avg;
    avg = sum / 3.00;
    printf("Average: %.2f", avg);
    return 0;
}